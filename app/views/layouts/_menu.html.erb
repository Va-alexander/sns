<div id="main-menu">
	<ul>
		<%t('menu.items').each do |item| %>
		  <li> <%= link_to "#{item}", root_path(anchor: "#{item}") %></li> 
		  <!-- To Do Add last item class to menu items -->
		<% end %>  
	</ul>
</div>